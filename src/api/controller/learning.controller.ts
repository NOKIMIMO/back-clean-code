import { Request, Response } from "express";

export const getTodayQuizz = async (
  req: Request,
  res: Response,
): Promise<void> => {
  
};

export const answerCard = async (
  req: Request,
  res: Response,
): Promise<void> => {
  
};
